
<li class="nav-item ps-3">
    <a class="nav-link"
       href="#"
       @onclick="async () => await HandleOnSourceSelectedAsync(Source)"
       @onclick:preventDefault
       @onclick:stopPropagation>
        <span class="oi oi-document" aria-hidden="true"></span> @Source.Source.Title
    </a>

    @if (_feeds != null)
    {
        <ul class="nav flex-column">
            @foreach (var feed in _feeds)
            {
                <li class="nav-item ps-3">
                    <a class="nav-link"
                       href="#"
                       @onclick="async ()=> await HandleOnFeedSelectedAsync(feed)"
                       @onclick:preventDefault
                       @onclick:stopPropagation>
                        @feed.Feed.Title
                    </a>
                </li>
            }
        </ul>
    }
</li>
